# Git 提交安全检查 - Key 泄露分析

## 检查结果

### 🔴 严重问题：`.env.example` 包含真实 API Key

**文件**: `.env.example` (已修改，未暂存)

```
NEWAPI_API_KEY=sk-cUXQjxWXsRStlbYztGT5WrDMlRZf5bDZxkXfEe9NRkpkO6gL
```

这是一个**真实的 API Key**，不是占位符。如果提交，会直接泄露到仓库中。

同文件中其他 key 均为占位符（`your_xxx_api_key_here`），只有这一个泄露了。

---

### 🟡 已有问题：`CLEANUP_SUMMARY.md` 包含旧 Key

**文件**: `CLEANUP_SUMMARY.md` (已跟踪，不在本次 diff 中)

第 34 行引用了一个已被移除的 key：
```
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
```

此文件已在 git 历史中，非本次新增泄露，但建议清理。

---

### ✅ 安全的文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `.env` | ✅ 已忽略 | `.gitignore:144` 正确排除 |
| `config.py` | ✅ 已忽略 | `.gitignore:154` 正确排除 |
| `CONFIG.md` | ✅ 安全 | 仅含占位符 `sk-or-v1-xxxxxxxxxxxxx` |
| `plan/evaluator_model_settings.md` | ✅ 安全 | 仅含占位符 `sk-xxxxxxxxxxxxxxxxxxxxxxxx` |
| `modules/*.py` | ✅ 安全 | 所有 key 从 `.env` 读取，无硬编码 |
| `llm_client.py` | ✅ 安全 | key 通过 `config` 模块读取 |
| `app.py` / `database.py` / `background_tasks.py` / `ui_pages.py` | ✅ 安全 | 不含 key |

### 📋 其他注意事项

- `eval_results.db` 是已跟踪的二进制文件，修改后会包含新的评估数据，但不含 API key
- `modules/` 目录从 `.gitignore` 中移除（故意的），模块文件本身不含硬编码 key
- `2026-02-12` 文件被删除，无影响

---

## 修复计划

### 步骤 1：修复 `.env.example` 中的真实 Key

将 `.env.example` 第 75 行：
```
NEWAPI_API_KEY=sk-cUXQjxWXsRStlbYztGT5WrDMlRZf5bDZxkXfEe9NRkpkO6gL
```
替换为：
```
NEWAPI_API_KEY=your_newapi_api_key_here
```

### 步骤 2（可选）：清理 `CLEANUP_SUMMARY.md` 中的旧 Key

将第 34 行中的真实 key 替换为掩码：
```
- 移除了硬编码的 OpenRouter API 密钥: `sk-or-v1-xxxx...xxxx`
```

### 步骤 3：验证后提交

修复后可以用 `git diff .env.example` 确认 key 已被替换，然后再提交。
